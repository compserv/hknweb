name: Formatting check for Python files

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/setup_test_environment.yaml
      - name: Run formatting check
        run: black --check
